# evolver-controller

Extracted edge runtime preserving durable EdgeStore, authenticated sync,
generation fencing, orphan behavior, simulator, installer, updater, and the
existing CLI/service entry points. Production deployment remains systemd.

Origin: Meta WebUI `wire-in-cli` `652cc5d`.
