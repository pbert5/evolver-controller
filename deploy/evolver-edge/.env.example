# Bootstrap-only transport configuration. Enrollment tokens must be supplied
# out-of-band and are never committed here.
EVOLVER_SERVER_URL=
EVOLVER_EDGE_STATE_ROOT=/var/lib/evolver-controller
EVOLVER_EDGE_HARDWARE_STATE_ROOT=/var/lib/evolver-hardware
EVOLVER_EDGE_RUNTIME_ROOT=/run/evolver-controller
