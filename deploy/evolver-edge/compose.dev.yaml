services:
  evolver-controller:
    build:
      context: ../../evolver/evolver-controller
    environment:
      EVOLVER_CONTROLLER_VERSION: dev
    command: ["--interval", "2"]
  evolver-hardware:
    build:
      context: ../../evolver/evolver-hardware
    command: ["--interval", "2"]
